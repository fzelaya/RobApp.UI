# data/

This directory holds `strategy-reports.xlsx` — the single shared workbook
that every extraction (from the web upload page *and* the CLI) appends a
row to.

The file is created automatically on first run if it doesn't exist yet
(header row only). This README just keeps the empty folder present when
the project is cloned or unzipped fresh.
